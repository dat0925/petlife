# ペットサロンAIエージェント「PetAgent」要件定義書

作成日: 2026-07-15
バージョン: 1.0 (MVP)

## 1. 背景と目的

- 当社はトリミングサロン検索・予約ポータル「EPARKペットライフ」(https://petlife.asia/) を運営している。
- トリミング業界最大のクレーム要因は「仕上がりがイメージと違う」こと。飼い主とトリマーの間でカットスタイルの認識合わせが難しい。
- 海外では AI受付・スタイルプレビュー(PetHairstyle AI 等)が急成長しているが、日本には EPARK のような予約ポータルと連動した「仕上がりシミュレーション → 予約」の一気通貫サービスは存在しない。

**目的:** 飼い主が愛犬の写真からトリミング後の仕上がりイメージをAIで生成し、そのまま EPARKペットライフの予約導線につなげる Web アプリを提供する。集客フックとして機能させ、ポータルへの送客を増やす。

## 2. ターゲットユーザー

- **プライマリ:** 犬の飼い主(トリミングサロン利用者・検討者)。スマホ利用が中心。
- セカンダリ(将来): サロンスタッフ(カウンセリング時の合意形成ツールとして)。

## 3. スコープ

### MVP(今回実装)

| # | 機能 | 内容 |
|---|------|------|
| F1 | 写真アップロード | 飼い犬の写真をアップロード(スマホカメラ起動対応)。JPEG/PNG/WebP、最大10MB |
| F2 | AI犬種・毛質判定 | Claude API (vision) が犬種・毛質・現在の毛の長さを判定し、犬種に適したカットスタイルを提案 |
| F3 | トリミングメニュー選択 | 定番スタイル(テディベアカット、サマーカット、ラムクリップ、ライオンカット等)から選択。F2の判定結果でおすすめ表示 |
| F4 | 仕上がり画像生成 | 画像編集AI (Gemini) が「同じ犬の施術後」イメージを生成。Before/After比較表示 |
| F5 | 予約導線 | 「このスタイルでサロンを予約」ボタン → EPARKペットライフのサロン検索へ遷移 |
| F6 | デモモード | APIキー未設定時もUI/フローを確認できるモック応答 |

### 次フェーズ先行モック(実装済み)

| # | 機能 | 内容 |
|---|------|------|
| M1 | 埋め込みウィジェット | `/embed` — EPARKペットライフの施設情報掲載画面に iframe で埋め込むコンパクト版シミュレーター。生成結果は `postMessage`(`type: "petagent:attach"`)で親ページへ連携 |
| M2 | 施設ページ統合デモ | `/epark-mock` — EPARK施設情報ページのモック。埋め込みシミュレーターで生成した仕上がりイメージが予約フォームに自動添付され、予約リクエスト(モック)にスタイル名+画像が引き渡される |

本番展開時の想定: petlife.asia の施設詳細ページに iframe を設置し、`postMessage` のオリジン検証(`https://petlife.asia` ⇔ PetAgent 配信オリジン)を相互に行った上で、添付画像を予約APIのパラメータ(またはS3等への一時アップロード+参照ID)として渡す。

### スコープ外(次フェーズ候補)

- LINE公式アカウント連携(LINE Messaging API でのチャットボット化)
- EPARK予約APIとの直接接続(モックM2の本実装。スタイル画像を予約情報に永続添付)
- サロン向け管理画面(生成画像をカルテ・指示書として利用)
- AI受付エージェント(問い合わせ・予約変更の自動応対)
- 猫対応

## 4. システム構成

```
[ブラウザ(スマホ/PC)]
   │ 写真アップロード
   ▼
[Next.js (App Router, TypeScript)]
   ├─ POST /api/analyze  … Claude API (claude-opus-4-8)
   │     画像 → 犬種/毛質判定 + おすすめスタイル (structured outputs)
   ├─ POST /api/generate … Gemini 画像編集 API
   │     画像 + スタイル指定 → 施術後イメージ生成
   └─ 予約CTA → https://petlife.asia/salon/ へ遷移
```

- **フレームワーク:** Next.js 15 (App Router) / TypeScript / Tailwind CSS
- **AI(判定・提案):** Anthropic Claude API `claude-opus-4-8`(vision + structured outputs、adaptive thinking)
- **AI(画像生成):** Google Gemini `gemini-2.5-flash-image`(画像編集。Claudeは画像生成非対応のため)
- **環境変数:** `ANTHROPIC_API_KEY`, `GEMINI_API_KEY`(未設定時はデモモード)
- 画像はサーバーに保存しない(メモリ内処理のみ。個人情報保護の観点)

## 5. 画面フロー

1. **トップ:** サービス説明 + 写真アップロード(カメラ/ギャラリー)
2. **判定結果:** 犬種・毛質と、おすすめスタイルバッジ付きのメニュー一覧
3. **生成中:** ローディング演出
4. **結果:** Before/After 比較 + 「このスタイルでサロンを予約(EPARKペットライフ)」CTA + 別スタイル再生成

## 6. トリミングメニュー(初期データ)

| ID | スタイル | 説明 | 主な対象犬種 |
|----|---------|------|-------------|
| teddy | テディベアカット | 顔を丸く、ぬいぐるみ風 | トイプードル、ビションフリーゼ等 |
| summer | サマーカット | 全身短め、夏向け | 全犬種 |
| lamb | ラムクリップ | 体は短く脚は太めに | プードル系 |
| lion | ライオンカット | 顔まわりにたてがみ風 | ポメラニアン等 |
| puppy | パピーカット | 全体を均一に短く自然に | シーズー、マルチーズ等 |
| top-knot | トップノット | 頭頂部を結ぶ | ヨークシャーテリア、シーズー等 |

## 7. 非機能要件

- スマホファースト(375px〜)レスポンシブ
- 画像生成の応答目標: 30秒以内(ローディングUI必須)
- 生成画像には「AIによるイメージです。実際の仕上がりとは異なる場合があります」の注記を常時表示(クレーム予防)
- アップロード画像・生成画像は永続化しない
- エラー時は日本語のわかりやすいメッセージとリトライ導線

## 8. 成功指標(KPI案)

- シミュレーション実行数 / 完了率
- 「予約する」CTAクリック率(EPARKペットライフへの送客数)
- 再生成率(スタイル比較のエンゲージメント)
